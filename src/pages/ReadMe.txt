Basic setup to work with react & mobx

Create store for the application


observable -- is while creatig value (which we wann to watch out for evey changes)

autorun - is will keep exceuting whenever there is update in store

observer - to print values in component that we made variables observable